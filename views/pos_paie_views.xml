<odoo>
    <!-- Périodes de paie -->
    <record id="view_pos_paie_periode_tree" model="ir.ui.view">
        <field name="name">pos.paie.periode.tree</field>
        <field name="model">pos.paie.periode</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="date_debut"/>
                <field name="date_fin"/>
                <field name="total_commandes"/>
                <field name="total_cash"/>
                <field name="total_bp"/>
                <field name="commission_total"/>
                <field name="montant_net_total"/>
                <field name="state" widget="tag"/>
            </tree>
        </field>
    </record>

    <record id="view_pos_paie_periode_form" model="ir.ui.view">
        <field name="name">pos.paie.periode.form</field>
        <field name="model">pos.paie.periode</field>
        <field name="arch" type="xml">
            <form string="Période de paie">
                <header>
                    <button name="action_recompute" type="object" string="Recalculer" class="oe_highlight"/>
                    <button name="action_confirmer_paies_periode" type="object" string="Confirmer toutes les paies" class="btn-success" confirm="Êtes-vous sûr de vouloir confirmer toutes les paies de cette période ? Les commandes associées seront marquées comme payées."/>
                    <field name="state" widget="statusbar" statusbar_visible="confirm,done,cancel" class="oe_inline"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="date_debut"/>
                            <field name="date_fin"/>
                        </group>
                        <group string="Totaux">
                            <field name="total_commandes" readonly="1"/>
                            <field name="total_cash" readonly="1"/>
                            <field name="total_bp" readonly="1"/>
                            <field name="commission_total" readonly="1"/>
                            <field name="montant_net_total" readonly="1"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Vendeurs">
                            <field name="ligne_ids">
                                <tree editable="bottom">
                                    <field name="vendeur_card"/>
                                    <field name="vendeur_id"/>
                                    <field name="nb_commandes"/>
                                    <field name="total_commandes"/>
                                    <field name="total_bp"/>
                                    <field name="pourcentage"/>
                                    <field name="commission"/>
                                    <field name="montant_net"/>
                                </tree>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_pos_paie_periode" model="ir.actions.act_window">
        <field name="name">Périodes de paie</field>
        <field name="res_model">pos.paie.periode</field>
        <field name="view_mode">tree,form</field>
    </record>

    <menuitem id="menu_pos_paie_root" name="Paie" parent="pos_caisse.menu_pos_caisse_root" sequence="90" groups="pos_paie.group_pos_paie_user,pos_paie.group_pos_paie_manager"/>
    <menuitem id="menu_pos_paie_periodes" name="Périodes de paie" parent="menu_pos_paie_root" action="action_pos_paie_periode" sequence="10" groups="pos_paie.group_pos_paie_user,pos_paie.group_pos_paie_manager"/>
    <!-- Wizard form view -->
    <record id="view_pos_paie_wizard_form" model="ir.ui.view">
        <field name="name">pos.paie.wizard.form</field>
        <field name="model">pos.paie.wizard</field>
        <field name="arch" type="xml">
            <form string="Calcul de la paie">
                <header>
                    <field name="paiement_state" widget="statusbar" statusbar_visible="draft,ready,done" class="oe_inline"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="vendeur_id"/>
                            <field name="date_debut"/>
                            <field name="date_fin"/>
                            <field name="pourcentage" widget="percentage"/>
                        </group>
                        <group string="Résultat">
                            <field name="total_commandes" readonly="1"/>
                            <field name="montant_net" readonly="1"/>
                        </group>
                    </group>
                </sheet>
                <footer>
                    <button string="Fermer" class="btn-secondary" special="cancel"/>
                    <button name="action_prepare_sortie_caisse" type="object" string="Préparer sortie de caisse" class="oe_highlight"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Action to open the payroll wizard from a vendor -->
    <record id="action_pos_paie_wizard" model="ir.actions.act_window">
        <field name="name">Calculer paie</field>
        <field name="res_model">pos.paie.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="context">{"default_vendeur_id": active_id}</field>
    </record>

    <!-- Action without context for menu usage (no active_id reference) -->
    <record id="action_pos_paie_wizard_blank" model="ir.actions.act_window">
        <field name="name">Calculer paie</field>
        <field name="res_model">pos.paie.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

    <!-- Add button to pos.caisse.vendeur form via inheritance (inside sheet to avoid header dependency) -->
    <record id="view_pos_caisse_vendeur_form_inherit_paie" model="ir.ui.view">
        <field name="name">pos.caisse.vendeur.form.inherit.paie</field>
        <field name="model">pos.caisse.vendeur</field>
        <field name="inherit_id" ref="pos_caisse.view_pos_caisse_vendeur_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form/sheet" position="inside">
                <group string="Paie">
                    <button name="%(action_pos_paie_wizard)d" type="action" string="Calculer Paie" class="oe_highlight" groups="pos_paie.group_pos_paie_user"/>
                </group>
            </xpath>
        </field>
    </record>

    <!-- Paie Vendeur - Tree View (uses inherited fields from pos.caisse.vendeur via _inherits) -->
    <record id="view_pos_paie_vendeur_tree" model="ir.ui.view">
        <field name="name">pos.paie.vendeur.tree</field>
        <field name="model">pos.paie.vendeur</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="carte_numero"/>
                <field name="total_commandes"/>
                <field name="montant_paye"/>
                <field name="pourcentage"/>
                <field name="date_paiement"/>
            </tree>
        </field>
    </record>

    <!-- Paie Vendeur - Form View -->
    <record id="view_pos_paie_vendeur_form" model="ir.ui.view">
        <field name="name">pos.paie.vendeur.form</field>
        <field name="model">pos.paie.vendeur</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_open_wizard" type="object" string="Assistant de Paie" class="oe_highlight"/>
                    <button name="action_prepare_sortie_caisse" type="object" string="Préparer sortie de caisse"/>
                    <button name="action_confirmer_paie" type="object" string="Confirmer Paie" class="btn-success" confirm="Êtes-vous sûr de vouloir confirmer cette paie ? Les commandes associées seront marquées comme payées." />
                    <field name="state" widget="statusbar" statusbar_visible="non_payee,payee" class="oe_inline"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="vendor_id"/>
                            <field name="name" readonly="1"/>
                            <field name="carte_numero" readonly="1"/>
                        </group>
                        <group>
                            <field name="total_commandes" readonly="1"/>
                            <field name="montant_paye"/>
                            <field name="pourcentage"/>
                            <field name="date_paiement"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Commandes">
                            <field name="commande_ids">
                                <tree editable="bottom">
                                    <field name="vendeur_card" invisible="1"/>
                                    <field name="commande_id"/>
                                    <field name="montant"/>
                                    <field name="date"/>
                                </tree>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>
</odoo>
